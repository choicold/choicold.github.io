<!--
위키 글 템플릿 (3층 구조 + 적용기는 Blog로 위임)

쓰는 법:
  1. 이 파일을 src/content/wiki/<카테고리>/<주제>.md 로 복사
     (폴더 = 카테고리. 예: src/content/wiki/PostgreSQL/pg_trgm.md)
  2. 이 주석 블록은 지우고 frontmatter부터 시작

계층 (위에서 아래로 깊어짐):
  · 1층 옵션 맵 (조망)   — "뭐가 있나" 카탈로그 표 + 상황별 선택. 다시 볼 때 여기부터.
  · 2층 개념과 원리 (복기) — 어떤 원리로 동작하나. 다이어그램 + 한 줄 요약.
  · 3층 코드 딥다이브 (아카이브) — 소스/내부 구현 레벨. 깊이 판 내용 보존.
  · 적용기 (Blog로 위임)  — "우리 사례 + 재현 쿼리"는 Experience라 위키엔 링크만.

확장자: 기본 .md. 본문에 Astro/JSX 컴포넌트를 넣어야 할 때만 .mdx.

앵커(점프 링크) 원리:
  - heading은 자동으로 slug id가 박힌다. 예) "## 2층 · 개념과 원리" → id="2층--개념과-원리"
  - 1층 표에서  [원리 →](#2층--개념과-원리)  형태로 아래 층으로 점프
  - {#custom-id} 문법은 이 프로젝트 파이프라인에서 안 먹으니 쓰지 말 것
  - 제목을 바꾸면 slug가 바뀌어 점프가 깨질 수 있음
    → npm run build 가 깨진 앵커를 잡아 빌드를 실패시킨다(scripts/check-anchors.mjs)
  - 섹션 제목은 유니크하게(중복 "왜" 금지 → "왜 부분일치는 인덱스를 못 타나")

다이어그램:
  - 이 블로그는 mermaid 미지원. 다이어그램은 코드블록 안 ASCII 도식으로.

적용기:
  - "우리 사례/트러블슈팅/재현 쿼리"는 Blog에 쓰고 위키는 링크만.
  - 블로그 글이 아직 없으면 링크 대신 "(블로그 글 예정)" 문구로 둔다.
    실재하지 않는 /posts 링크를 넣으면 빌드 앵커검증이 깨진 링크로 잡는다.
-->

---
title: ""
description: ""
# publishDate 는 선택. 화면에 시각은 안 보이고 날짜만 보이게 운영.
# publishDate: "2026-06-03T00:00:00+09:00"
---

> **TL;DR.** (이 기술을 한 줄로 — 다시 볼 때 제일 먼저 읽는 문장)

## References

- 📄 공식문서 — [제목](URL)
- 💻 소스코드 — [경로 (GitHub)](URL)

## 1층 · 옵션 맵

뭐가 있나 한눈에.

| 항목 | 한 줄 목적 | 자세히 |
| --- | --- | --- |
| `옵션A` | 무엇을 위한 것인지 | [원리 →](#2층--개념과-원리) |
| `옵션B` | ... | [구현 →](#3층--코드-딥다이브) |

**상황별 선택**

| 이런 상황이면 | 이걸 쓴다 |
| --- | --- |
| (조건) | `옵션A` |
| (조건) | `옵션B` |

## 2층 · 개념과 원리

어떤 원리로 그 문제를 푸는가. (내가 이해한 기저 원리 본체)

```text
(ASCII 도식 — 예: 입력 → 변환 → 매칭 흐름)
```

- 핵심 1: (한 줄 요약)
- 핵심 2: (한 줄 요약)

## 3층 · 코드 딥다이브

소스/내부 구현 레벨. 실제 코드와 동작.

```sql
-- 또는 java 등. 핵심 코드 / 내부 인터페이스.
```

## 적용기 · 우리 사례

> 실제 고른 조합·이유와 재현 쿼리는 블로그(Experience)에 정리합니다.
> (블로그 글 예정)
<!-- 블로그 글이 생기면 위 줄을 아래로 교체:
[적용기·재현 쿼리 →](/posts/<슬러그>/#<블로그-섹션-앵커>) -->
