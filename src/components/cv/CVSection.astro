---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<section class="mt-12">
	<h2 class="text-accent-2 mb-4 border-b border-current/15 pb-2 text-xl font-semibold">
		{title}
	</h2>
	<slot />
</section>
